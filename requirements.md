# Kanban Board 需求規格書

> **用途**：個人專案管理看板，部署於 GitHub Pages
> **GitHub Username**：`PosenChen`
> **部署網址**：`https://posenchen.github.io/kanban/`

---

## 1. 總覽頁面（甘特圖模式）

### 1.1 時間軸（橫軸）
- 最上方為日期標頭，可縮放至 **月／週／日** 粒度
- 日期標頭應清晰顯示日期與星期，方便快速定位

### 1.2 專案列（縱軸）
- 每一列代表一個「專案」或「行事曆活動」
- 以水平長條顯示該專案的 **起始日期～結束日期** 範圍
- 長條的顏色依據專案狀態顯示：
  - 準備中：灰色／中性色
  - 進行中：藍色／亮色
  - 已完成：綠色
- 長條長度與實際日期範圍成比例

### 1.3 互動功能
- **縮放控制**：可切換月／週／日視圖
- **搜尋／篩選**：依專案名稱、標籤、狀態、優先級篩選
- **點擊長條**：跳轉至該專案的詳細頁面（或展開側邊面板）

---

## 2. 專案詳細頁面

### 2.1 基本資訊
- 專案名稱、描述
- 起始日期、結束日期
- 實際開始日期、實際結束日期（若有）
- 進度百分比
- 狀態（準備中／進行中／已完成）
- 優先級（低／中／高）
- 標籤（可多選）

### 2.2 專案狀態管理
- 使用者可手動更新專案狀態
- 每筆更動會記錄最後更新時間

### 2.3 子專案層級
- 支援 **無限層級** 的父子專案關係
- 每個專案可以擁有零個或多個子專案

---

## 3. 專案層級結構

### 3.1 層級示意
```
專案 A（大專案）
├── 子專案 B（階段：招標準備）
│   ├── 子專案 B1（契約書撰寫）
│   └── 子專案 B2（底價訂定）
└── 子專案 C（階段：採購執行）
```

### 3.2 功能需求
- 總覽甘特圖中，子專案會以縮排顯示在父專案下方
- 父專案長條的日期範圍應自動涵蓋其所有子專案
- **可展開／收合** 父子關係，方便檢視不同粒度
- 點擊父專案可展開其所有子專案細節

### 3.3 狀態彙整
- 父專案的狀態應反映其子專案的狀態：
  - 若任一子專案為「進行中」，父專案也視為「進行中」
  - 若所有子專案皆為「已完成」，父專案可標記為「已完成」

---

## 4. 日期下探視圖

### 4.1 功能描述
- 在甘特圖上 **點擊任意日期**，可下探至「日曆視圖」
- 該視圖顯示 **該日期所有專案的即時狀態**

### 4.2 顯示內容
- 當天所有專案的名稱、狀態、進度
- 當天預計執行的活動清單
- 哪些專案處於「進行中」
- 是否有專案在今天到期

### 4.3 互動功能
- 可切換日期（前一天／後一天／回到今天）
- 可直接點擊專案從日曆視圖跳轉到專案詳細頁面
- 可新增當天活動

---

## 5. 專案 CRUD（新增、編輯、刪除）

### 5.1 新增專案
- 使用者可新增專案，需填寫：
  - 專案名稱（必填）
  - 描述（選填）
  - 父專案（選填，建立層級關係）
  - 起始日期、結束日期
  - 狀態、優先級
  - 標籤

### 5.2 編輯專案
- 可編輯上述所有欄位
- 支援拖曳甘特圖長條以調整日期範圍
- 可改變父專案、狀態、標籤等

### 5.3 刪除專案
- 刪除專案時，需確認是否同時刪除其子專案
- 刪除前應彈出確認對話框

---

## 6. 資料持久化

### 6.1 本地儲存
- 所有專案資料儲存在瀏覽器 **LocalStorage**
- 支援多分頁 / 多裝置同步（透過 `storage` event）
- 首次開啟自動填入範例資料

### 6.2 GitHub 同步（可選）
- 使用者可輸入 GitHub Personal Access Token
- 選擇是否啟用 GitHub 同步
- 同步目標：`PosenChen/kanban-data` 倉庫的 `data/projects.json`
- 支援手動匯出 JSON 備份與匯入還原
- 支援從 GitHub 倉庫匯入專案資料

---

## 7. UI 偏好與設計

### 7.1 控制項布局
- 當控制項過多時，使用 **可折疊面板**（例如「指標 ▾」）收起來
- 不需要全部攤開在工具列上

### 7.2 互動回饋
- 按壓按鈕時應有 hover 與 active 狀態
- 篩選結果即時回應（非同步非重整頁面）

### 7.3 無障礙
- 所有可互動元素應有明確的標籤與 role
- 支援鍵盤操作（Tab 導覽、Enter 確認）

---

## 8. 技術規格

| 項目 | 規格 |
|------|------|
| **框架** | React + TypeScript |
| **建構工具** | Vite（Vite 8） |
| **樣式** | Tailwind CSS v4 |
| **路由** | react-router-dom（HashRouter） |
| **部署** | GitHub Pages（GitHub Actions） |
| **本地儲存** | LocalStorage |
| **雲端同步** | GitHub API（可選） |

---

## 9. 尚未實作的功能（Roadmap）

1. **拖曳調整日期** — 目前甘特圖長條不可拖曳
2. **即時同步** — 目前僅手動 GitHub 同步
3. **提醒通知** — 專案到期提醒
4. **專案統計** — 依狀態／標籤的百分比圓餅圖
5. **手動日期修正** — 甘特圖長條拖曳編輯
6. **郵件匯出** — 將專案報告以 email 格式匯出
7. ~~**主題切換** — 支援暗色／亮色模式~~ ✅ 已實作（20260828）：右下角浮动切換鈕，預設跟隨系統偏好，選擇存 localStorage `kanban_theme`
8. **多國語言** — i18n 支援

---

*最後更新：2026-08-22*
